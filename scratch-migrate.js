const admin = require('firebase-admin');

// Ensure you have the correct service account or initialize via default env
const serviceAccount = require('./firebase-admin-key.json'); // wait, do I have the key?
